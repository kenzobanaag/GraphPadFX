package test.model.edge;

import org.junit.jupiter.api.Test;
import sketchpad.model.canvaselement.vertex.Node;
import sketchpad.model.canvaselement.vertex.Vertex;

import static org.junit.jupiter.api.Assertions.*;

class EdgesAdjacencyTest {

    /*
    * Test directed and undirected/ normal and looped
    * */

    @Test
    void getAdjacentNodes() {

        // note: All java fx widgets cause init errors when testing.

        // might need to mock?

        //Node node1 = new Vertex(100,100);

    }
}