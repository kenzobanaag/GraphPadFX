package sketchpad.model.algorithms.graph;

public class Bipartite {
    // check for odd numbered cycles first, bipartite cannot have odd cycles
    // since chromatic number algorithm is exhaustive,
}
